# LOG — hornof.org on Astro (Phase 6)

Append-only, one line per iteration. Newest at the bottom.

- 2026-07-08 — Gates cleared (PRD + eng doc, Luke). Stashed the abandoned VT/Swup experiment; branched `astro-rebuild` off clean `origin/main` (785d1b3). ROADMAP-astro + LOG created. Starting F14 (scaffold + passthrough proof).
- 2026-07-08 — F14: settled the dot-dir passthrough unknown in an isolated scratch build — Astro v5 copies `.`-prefixed dirs from `public/` to `dist/` intact (`.2013test/index.html` survived). No workaround needed; archives + eclipse can live in `public/` as-is. Proceeding to scaffold the real tree.
- 2026-07-08 — F14 DONE (PR 0). Scaffolded Astro v5.18 (static output, `.mjs` config so the CommonJS Playwright suite stays intact — dropped `type:module`). `git mv` archives + eclipse into `public/`. Real `astro build` → `/.2013/`, `/.2025/`, `/projects/eclipse/`, `/projects/eclipse/PROVENANCE.txt` all 200 with real content; encoded as `tests/astro-passthrough.spec.js` (5/5 green). Next: F15 parity port.
- 2026-07-08 — F15 DONE (PR 1, parity port). `BaseLayout.astro` (inline pre-paint theme resolver + favicon + verbatim `style.css` + theme-toggle behavior); ported index (sidebar · scroll-spy · Tardis · Breton egg · Projects · sign-off), projects wall (client-rendered from inlined JSON, still data-driven), built-with, eclipse-built, 404. `git mv style.css` + `work/img` → `public/`. `build.format:"file"` keeps the `/projects.html` `/built-with.html` `/eclipse-built.html` routes the suite + links depend on. The four JS behaviors ported as Astro `<script>` (theme in the layout; Tardis/Breton/scroll-spy on index; wall render on projects). Harness repointed to `npm run build && npm run preview` on :8788 (retired `server.js`). One test adapted (projects sync check reads built `dist/projects.html` — source moved to `.astro`; retires in F17). **79/79 green**; Lighthouse root 100/100, projects wall 100/100. Verified on LOCAL preview only (`astro preview` serves the `.2013`/`.2025` dot-dirs over HTTP — 200 + real content).
- 2026-07-08 — MERGE-TIME NOTE for Luke (do NOT auto-execute): the Cloudflare Pages project is no-build / root-publish today. Landing the Astro rebuild on `main` requires switching the Pages build to `npm run build` with publish dir `dist/`. That's Luke's infra step (like DNS), done at merge — it is intentionally NOT part of this branch's commits. Branch previews serve raw source, so F15 was gated on the local build + preview instead of a Pages preview URL.
- 2026-07-08 — F16 DONE-pending-feel-check (PR 2, transitions + persist). Added `<ClientRouter />` to BaseLayout `<head>`; promoted the panorama + scrim from `body::before/::after` to real `#bg-panorama`/`#bg-scrim` elements marked `transition:persist` (identical persist name on all 5 pages — verified). KEY FINDING: `transition:persist` keeps the node but re-inserting it during the swap RESTARTS a CSS `animation` (probed: panorama currentTime 517→0 across a nav) — persist alone does NOT preserve the breathing phase. Fix: drive the zoom via WAAPI in BaseLayout, capture `currentTime` on `astro:before-swap` and resume it on `astro:after-swap` → phase now carries continuously (test-verified). Re-wired every behavior for SPA swaps (Astro doesn't re-run module scripts): theme re-applied in `astro:after-swap` (persisted toggle keeps its wiring), Tardis/Breton/scroll-spy re-init on `astro:after-swap` with document/scroll listener teardown, projects wall re-renders. Passthrough links (archives `/.20xx/`, eclipse app) marked `data-astro-reload` so they full-load instead of SPA-swapping (keeps the Tardis 200-nav test honest + doesn't SPA-morph a Three.js app). Reduced motion: WAAPI zoom not started + a `::view-transition-*` `animation:none` guard in style.css → instant swap. New `transitions.spec.js` (4 tests): ClientRouter opt-in, real VT fires on internal nav (no full reload), panorama phase continuity across a nav, reduced-motion instant. Adapted one projects test (index→wall link is now a same-document swap, not a document GET — asserts URL + wall renders instead of a 200 response; strictly stronger). **83/83 green**; Lighthouse root 100/100, wall 99/100 (budget ≥95). The frost-flash quality of the pane cross-fade is NOT machine-verifiable (snapshots catch the settled DOM, not mid-transition frames) — the panorama+scrim are held live/out of the snapshot so only the panes cross-fade, but whether the frost reads clean awaits Luke's feel-check on the preview.
