{
  "name": "hornof-org",
  "version": "1.0.0",
  "description": "hornof.org — personal site on Astro, static output to Cloudflare Pages",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "serve": "node server.js",
    "test": "playwright test",
    "test:install": "playwright install --with-deps chromium"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "lighthouse": "^12.8.2"
  }
}
